
//import { Value } from "./base2";

/*
value := primitive | block
primitive := string | number | bigint | boolean | symbol

digit  := [0-9]

string := '"' string_char* '"'
number := [+-]? digit* '.' digit* ([eE][+-]?digit+)?
bigint := [+-]? digit+
boolean:= true | false
symbol := «todo»


block := ( name arglist )
name  := nonreserved_char+ | string

arglist := epsilon | arg arglist
arg   := value

*/

